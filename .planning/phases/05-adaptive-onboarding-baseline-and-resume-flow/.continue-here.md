---
phase: 05-adaptive-onboarding-baseline-and-resume-flow
task: 1
total_tasks: 5
status: in_progress
last_updated: 2026-03-25T12:19:27.755Z
---

<current_state>
We are pausing in the middle of re-planning Phase 5 after the original planning attempt failed the GSD checker. The old Phase 5 plan files were written in a freeform structure and were rejected for missing required frontmatter (`phase`, `type`, structured `must_haves`) and missing XML `<task>` blocks with task-level verify steps.

At pause time, `05-01-PLAN.md` has already been fully rewritten into the valid GSD schema as a **Wave 0 foundation plan**. It now covers the UI-SPEC prerequisite work: local shadcn initialization contract (`frontend/components.json`), required onboarding UI primitives under `frontend/src/components/ui/`, and `frontend/src/pages/Onboarding.test.tsx` required by Nyquist validation.

`05-02-PLAN.md` and `05-03-PLAN.md` still contain the older invalid structure and must be replaced completely before verification is rerun.
</current_state>

<completed_work>

- Task 1: Re-diagnosed the failed planning attempt — Done
  - Confirmed checker blockers were real: invalid plan schema, oversized plans, missing Wave 0 test work, missing ONB-04 auth-entry coverage, broken recommendation->confirm contract, and missing task-level automated verification.

- Task 2: Identified the correct valid GSD plan schema — Done
  - Read `04-01-PLAN.md`, `04-02-PLAN.md`, and `04-03-PLAN.md`.
  - Confirmed required structure: frontmatter with `phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, structured `must_haves`; body with `<objective>`, `<execution_context>`, `<context>`, `<tasks>`, XML `<task>` blocks, `<verification>`, `<success_criteria>`, and `<output>`.

- Task 3: Gathered Phase 5-specific planning inputs — Done
  - Read `05-CONTEXT.md`, `05-UI-SPEC.md`, `05-VALIDATION.md`, `AuthCallback.tsx`, `Login.tsx`, `authStore.ts`, and existing onboarding/recommendation context.
  - Confirmed the auth entry flow currently routes based only on `user.isNewUser`, which is insufficient for ONB-04 partial-resume coverage.
  - Confirmed UI-SPEC Wave 0 requires local shadcn initialization and official component primitives.
  - Confirmed VALIDATION explicitly requires `frontend/src/pages/Onboarding.test.tsx`.

- Task 4: Rewrote `05-01-PLAN.md` into valid schema — Done
  - New `05-01-PLAN.md` is a Wave 0 plan.
  - It covers `frontend/components.json`, `frontend/src/components/ui/*` primitives, `frontend/src/test/renderWithProviders.tsx`, and `frontend/src/pages/Onboarding.test.tsx`.
  - It includes structured `must_haves`, XML tasks, and task-level `<verify><automated>` commands.

- Task 5: Rewriting remaining valid Phase 5 plans — In progress
  - Not started yet for the actual rewrite work.
</completed_work>

<remaining_work>

- Rewrite `05-02-PLAN.md` from scratch using the valid GSD schema.
  - This should likely become the main backend execution plan (or split into two smaller plans if scope still looks too large) covering:
    - backend onboarding status endpoint
    - round 2 / round 3 questions + DTOs + sequential gating
    - learner profile creation/update hooks across rounds
    - recommendation requiring round 3
    - recommendation payload must include `learningPathId` for D-17
    - backend specs / task-level verify commands

- Rewrite `05-03-PLAN.md` from scratch using the valid GSD schema.
  - It must explicitly cover ONB-04 entry flow before `/onboarding`, including:
    - `frontend/src/pages/Login.tsx`
    - `frontend/src/pages/AuthCallback.tsx`
    - `frontend/src/stores/authStore.ts`
    - any onboarding-status-driven redirect logic needed so partial users go back to `/onboarding` instead of dashboard
  - It must also cover:
    - onboarding container + child components
    - dedicated hooks (including question-loading hook, not inline `api.get` in page)
    - stepper / resume banner / round transitions
    - confirm flow using backend-provided `learningPathId`
    - frontend test expansion and task-level verify commands

- Decide whether backend/frontend work still needs to be split into more than two plans.
  - The earlier checker flagged oversize plans.
  - If either rewrite still looks too broad, split further before verification.

- Run plan checker again after all rewritten plans exist.
  - Iterate until checker passes or remaining issues are very small and explicit.
</remaining_work>

<decisions_made>

- Decided to abandon the previous freeform Phase 5 plans and rewrite them using the exact Phase 4 GSD schema.
  - Reason: the checker failure was fundamentally about plan format and execution-readiness, not just small content gaps.

- Decided to make Wave 0 a dedicated plan (`05-01-PLAN.md`).
  - Reason: both `05-UI-SPEC.md` and `05-VALIDATION.md` require shadcn initialization and `Onboarding.test.tsx` before later UI work. This is not optional.

- Decided that ONB-04 must be planned end-to-end from auth entry points, not only inside `/onboarding`.
  - Reason: checker explicitly flagged missing coverage for partial users who login/OAuth and would otherwise be routed to dashboard.

- Decided that D-17 must be implemented literally in the plans using `learningPathId`, not slug.
  - Reason: previous plan tried to pass slug and leave backend resolution as a future assumption, which violated locked decision D-17.
</decisions_made>

<blockers>
- Current blocker: `05-02-PLAN.md` and `05-03-PLAN.md` are still invalid and must be replaced before plan verification can succeed.
  - Workaround: rewrite them entirely from scratch; do not patch the old freeform structure.

- Current blocker: old scope split may still be too large for checker acceptance.
  - Workaround: while rewriting, keep each plan narrow and explicitly split backend/frontend concerns (and split again if needed).
</blockers>

<context>
Mental model when pausing:
- The important discovery was that the previous planning content had many useful ideas, but the format was incompatible with GSD execution and checker expectations.
- The safest resume strategy is **not** to salvage the old prose plans. Instead, use `04-01`, `04-02`, and `04-03` as structural templates and rewrite the remaining Phase 5 plans cleanly.
- Treat checker blockers as a checklist:
  1. valid schema
  2. Wave 0 covered
  3. ONB-04 entry routing covered
  4. D-17 `learningPathId` contract covered
  5. task-level automated verify present
  6. scopes small enough
- 05-01 is already rewritten and should be preserved.
- Be careful not to invent files/packages beyond what exists. `frontend/src/lib/utils.ts` exists. `frontend/components.json` does not yet exist. `frontend/src/components/ui/*` also does not yet exist. This is exactly why Wave 0 was needed.
</context>

<next_action>
Start with: open `05-02-PLAN.md`, delete/replace the old freeform content, and rewrite it in the exact valid GSD schema modeled after `04-01-PLAN.md` / `04-02-PLAN.md`, focusing on backend multi-round onboarding status + submission + learner profile progression + recommendation payload with `learningPathId`.
</next_action>
