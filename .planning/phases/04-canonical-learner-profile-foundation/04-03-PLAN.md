---
phase: 04-canonical-learner-profile-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - backend/src/modules/learner-profile/learner-profile.service.ts
  - backend/src/modules/learner-profile/learner-profile.service.spec.ts
  - backend/src/modules/lessons/lessons.module.ts
  - backend/src/modules/lessons/lessons.service.ts
  - backend/src/modules/lessons/lessons.service.spec.ts
autonomous: true
requirements:
  - PROF-03
must_haves:
  truths:
    - "After lesson completion, the canonical learner profile is recalculated synchronously before the request finishes."
    - "After a passed quiz, the canonical learner profile is recalculated synchronously before the request finishes."
    - "When a learning path completes, the recalculation receives both completion-derived topic signals and AI chat topic signals instead of leaving the profile frozen without the fourth D-13 signal source."
  artifacts:
    - path: "backend/src/modules/learner-profile/learner-profile.service.ts"
      provides: "Synchronous recalc entrypoint and incremental rule helpers"
      contains: "async recalculate(userId: string, event:"
    - path: "backend/src/modules/lessons/lessons.service.ts"
      provides: "Lesson and quiz flows wired to learner-profile recalc"
      contains: "learnerProfileService.recalculate"
    - path: "backend/src/modules/lessons/lessons.module.ts"
      provides: "Cross-module dependency on LearnerProfileModule"
      contains: "LearnerProfileModule"
  key_links:
    - from: "backend/src/modules/lessons/lessons.service.ts"
      to: "backend/src/modules/learner-profile/learner-profile.service.ts"
      via: "post-write synchronous recalculate calls"
      pattern: "learnerProfileService\\.recalculate"
    - from: "backend/src/modules/learner-profile/learner-profile.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "learnerProfile.upsert/update using event-specific queries"
      pattern: "learnerProfile\\.(upsert|update)"
    - from: "backend/src/modules/learner-profile/learner-profile.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "AI chat topic aggregation from AIInteractionLog.questionSummary"
      pattern: "aiInteractionLog\\.(findMany|groupBy)"
---

<objective>
Finish Phase 4 by adding synchronous learner-profile recalculation hooks to lesson and quiz milestone flows.

Purpose: Ensure the canonical learner profile evolves from real learning signals instead of freezing at onboarding time, as required by D-09 through D-16.
Output: Event-aware recalc logic, lessons-module wiring, and regression tests for lesson complete, quiz pass, and path completion hooks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-canonical-learner-profile-foundation/04-CONTEXT.md
@.planning/phases/04-canonical-learner-profile-foundation/04-RESEARCH.md
@.planning/phases/04-canonical-learner-profile-foundation/04-02-PLAN.md
@backend/src/modules/learner-profile/learner-profile.service.ts
@backend/src/modules/lessons/lessons.service.ts
@backend/src/modules/lessons/lessons.module.ts
@backend/src/modules/lessons/lessons.service.spec.ts
@backend/src/modules/progress/progress.service.ts
@backend/src/modules/ai-chat/context/ai-context.builder.ts
@backend/src/modules/ai-chat/ai-chat.service.ts
@backend/prisma/schema.prisma

<interfaces>
From `backend/src/modules/lessons/lessons.service.ts` current milestone points:
```typescript
async completeLesson(userId: string, slug: string): Promise<UserProgress>
async submitQuiz(userId: string, slug: string, answers: Record<string, string[]>)
private async advanceToNextLesson(tx: TxClient, userId: string, lessonId: string, learningPathId: string): Promise<void>
```

Path completion signal already exists inside `advanceToNextLesson()`:
```typescript
await tx.userLearningPath.update({
  where: { userId_learningPathId: { userId, learningPathId } },
  data: {
    currentLessonId: null,
    completedAt: new Date(),
  },
});
```

AI chat log shape from `backend/prisma/schema.prisma` and `backend/src/modules/ai-chat/ai-chat.service.ts`:
```typescript
model AIInteractionLog {
  userId          String   @map("user_id")
  sessionContext  String   @map("session_context")
  questionSummary String   @map("question_summary") @db.VarChar(500)
}

await this.prisma.aiInteractionLog.create({
  data: {
    userId,
    questionSummary: dto.message.substring(0, 500),
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement incremental learner-profile recalculation rules in the canonical service</name>
  <files>
    backend/src/modules/learner-profile/learner-profile.service.ts
    backend/src/modules/learner-profile/learner-profile.service.spec.ts
  </files>
  <read_first>
    backend/src/modules/learner-profile/learner-profile.service.ts
    backend/src/modules/learner-profile/learner-profile.service.spec.ts
    backend/src/modules/progress/progress.service.ts
    backend/src/modules/ai-chat/context/ai-context.builder.ts
    backend/src/modules/ai-chat/ai-chat.service.ts
    backend/prisma/schema.prisma
    .planning/phases/04-canonical-learner-profile-foundation/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `recalculate(userId, { type: 'LESSON_COMPLETED', lessonId })` updates `learningPace` using actual time versus `lesson.estimatedMins` per D-16 and refreshes `lastRecalculatedAt`.
    - Test 2: `recalculate(userId, { type: 'QUIZ_PASSED', quizId, lessonId })` updates `skillLevel`, `strengths`, and `weaknesses` from quiz/topic performance per D-14 and D-15.
    - Test 3: `recalculate(userId, { type: 'TRACK_COMPLETED', learningPathId })` updates `preferredTopics` and `lastRecalculatedAt` without deleting existing onboarding-derived career goal.
    - Test 4: during `TRACK_COMPLETED`, the service also reads recent `AIInteractionLog.questionSummary` rows, extracts topic tokens, and merges them into `preferredTopics` per D-13 instead of ignoring AI chat topics.
    - Test 5: if no learner profile exists yet, `recalculate(...)` returns without throwing and does not fabricate a new profile row, matching the pre-onboarding edge-case guidance in 04-CONTEXT.md.
  </behavior>
  <action>Extend `backend/src/modules/learner-profile/learner-profile.service.ts` with a public method `async recalculate(userId: string, event: { type: 'LESSON_COMPLETED'; lessonId: string } | { type: 'QUIZ_PASSED'; quizId: string; lessonId: string } | { type: 'TRACK_COMPLETED'; learningPathId: string })`. Per D-10, keep recalculation synchronous and inside the request path, but per D-11 keep it incremental: query only the data needed for the triggered fields. Use these concrete rules unless the current code structure requires equivalent helpers with the same outcomes: for `LESSON_COMPLETED`, load the lesson `estimatedMins` and the user’s `UserProgress.timeSpentSeconds`; set `learningPace` to `FAST` when `timeSpentSeconds <= estimatedMins * 60 * 0.8`, `SLOW` when `timeSpentSeconds >= estimatedMins * 60 * 1.2`, else `NORMAL`. For `QUIZ_PASSED`, compute the user’s average passed quiz score and distinct completed lessons count: set `skillLevel` to `ADVANCED` when average passed score >= 90 and completed lesson count >= 10, `INTERMEDIATE` when average passed score >= 80 and completed lesson count >= 5, else `BEGINNER`. Also aggregate strengths/weaknesses at track or learning-path topic granularity per the research recommendation: use the lesson’s parent track name as the topic bucket, add the track name to `strengths` when average passed score in that bucket >= 85, add it to `weaknesses` when average passed score in that bucket < 70, and ensure the arrays are unique string arrays. For `TRACK_COMPLETED`, load the completed learning path and its track names, merge those names into `preferredTopics`, then also read recent `AIInteractionLog` rows for that user (for example the latest 10-20 rows ordered by `createdAt desc`) and extract stable topic strings from `questionSummary` / `sessionContext` to satisfy D-13’s fourth signal source. Use a deterministic extraction strategy only: normalize to lowercase, split on commas/obvious delimiters, trim, drop empty strings, drop generic filler such as `help`, `question`, `lesson`, `track`, `path`, and merge only distinct topic tokens that are at least 3 characters. Do not call external AI services or invent sentiment scoring here; this phase only ingests existing chat topics into `preferredTopics`. In all cases, preserve the existing `careerGoal` from the learner profile row. If `learnerProfile.findUnique({ where: { userId } })` returns null, log/return quietly rather than creating a fake profile before round 1 exists, because D-04 says profile creation belongs to Phase 5’s round-1 completion call path. Update `learner-profile.service.spec.ts` to cover the exact event contracts and boundary values above, including at least one test proving AI chat topics are merged uniquely with path-derived topics.</action>
  <acceptance_criteria>
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `type: 'LESSON_COMPLETED'`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `type: 'QUIZ_PASSED'`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `type: 'TRACK_COMPLETED'`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `LearnerLearningPace.FAST`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `LearnerSkillLevel.INTERMEDIATE`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `lastRecalculatedAt`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `aiInteractionLog`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `questionSummary`
    - `backend/src/modules/learner-profile/learner-profile.service.spec.ts` contains `LESSON_COMPLETED`
    - `backend/src/modules/learner-profile/learner-profile.service.spec.ts` contains `QUIZ_PASSED`
    - `backend/src/modules/learner-profile/learner-profile.service.spec.ts` contains `TRACK_COMPLETED`
    - `backend/src/modules/learner-profile/learner-profile.service.spec.ts` contains `questionSummary`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend test -- learner-profile.service.spec.ts --runInBand</automated>
  </verify>
  <done>The canonical profile service can deterministically recalculate profile fields from the three locked milestone events and ingest AI chat topic signals required by D-13 for PROF-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire lesson completion, quiz pass, and path completion flows to synchronous profile recalculation</name>
  <files>
    backend/src/modules/lessons/lessons.module.ts
    backend/src/modules/lessons/lessons.service.ts
    backend/src/modules/lessons/lessons.service.spec.ts
  </files>
  <read_first>
    backend/src/modules/lessons/lessons.module.ts
    backend/src/modules/lessons/lessons.service.ts
    backend/src/modules/lessons/lessons.service.spec.ts
    backend/src/modules/learner-profile/index.ts
    backend/src/modules/learner-profile/learner-profile.service.ts
  </read_first>
  <behavior>
    - Test 1: after `completeLesson(...)` successfully commits progress, `learnerProfileService.recalculate(userId, { type: 'LESSON_COMPLETED', lessonId })` is called exactly once.
    - Test 2: when `advanceToNextLesson()` reaches end-of-path and sets `UserLearningPath.completedAt`, `completeLesson(...)` also calls `learnerProfileService.recalculate(userId, { type: 'TRACK_COMPLETED', learningPathId })`.
    - Test 3: after `submitQuiz(...)` stores a passing `QuizResult`, `learnerProfileService.recalculate(userId, { type: 'QUIZ_PASSED', quizId, lessonId })` is called; failing quizzes do not trigger this recalc.
    - Test 4: the lessons module imports `LearnerProfileModule` so Nest can inject `LearnerProfileService` without dependency-resolution errors.
  </behavior>
  <action>Update `backend/src/modules/lessons/lessons.module.ts` to import `LearnerProfileModule` alongside `AuthModule`. Inject `LearnerProfileService` into `LessonsService` via the constructor. In `completeLesson()`, keep the existing transaction for progress update + advance logic, then after the transaction resolves call `await this.learnerProfileService.recalculate(userId, { type: 'LESSON_COMPLETED', lessonId: lesson.id })`. Modify the completion flow so it can also detect whether the path was finished in that transaction; the cleanest approach is to have `advanceToNextLesson()` return `{ pathCompleted: boolean }` or equivalent explicit metadata rather than forcing the caller to re-query broadly. When `pathCompleted` is true, immediately call `await this.learnerProfileService.recalculate(userId, { type: 'TRACK_COMPLETED', learningPathId: enrollment.learningPathId })` after the lesson-complete recalc. In `submitQuiz()`, after creating the `QuizResult`, call the recalc service only when `passed === true`, with exact payload `{ type: 'QUIZ_PASSED', quizId: quiz.id, lessonId: lesson.id }`; do not trigger on failed quiz attempts because D-09 specifies quiz pass, not all attempts. Keep AI chat topic ingestion inside `LearnerProfileService.recalculate()` rather than duplicating any AI-chat reads in `LessonsService`; this task’s responsibility is only to trigger the right event payloads synchronously.</action>
  <acceptance_criteria>
    - `backend/src/modules/lessons/lessons.module.ts` contains `LearnerProfileModule`
    - `backend/src/modules/lessons/lessons.service.ts` contains `private readonly learnerProfileService: LearnerProfileService`
    - `backend/src/modules/lessons/lessons.service.ts` contains `type: 'LESSON_COMPLETED'`
    - `backend/src/modules/lessons/lessons.service.ts` contains `type: 'TRACK_COMPLETED'`
    - `backend/src/modules/lessons/lessons.service.ts` contains `type: 'QUIZ_PASSED'`
    - `backend/src/modules/lessons/lessons.service.ts` contains `if (passed)`
    - `backend/src/modules/lessons/lessons.service.spec.ts` contains `learnerProfileService.recalculate`
    - `backend/src/modules/lessons/lessons.service.spec.ts` contains `QUIZ_PASSED`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend test -- lessons.service.spec.ts learner-profile.service.spec.ts --runInBand</automated>
  </verify>
  <done>Profile recalculation is wired into the real lesson/quiz milestone flows synchronously and test-covered, satisfying PROF-03 without introducing queues or extra infrastructure.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend test -- learner-profile.service.spec.ts lessons.service.spec.ts --runInBand`
- `pnpm --filter backend exec tsc --noEmit`
</verification>

<success_criteria>
- Lesson completion, quiz pass, and path completion each trigger synchronous learner-profile recalculation.
- The recalc service updates only event-relevant profile fields and preserves pre-existing profile data.
- AI chat topic signals are ingested into `preferredTopics` during recalculation instead of being ignored.
- Failed quizzes do not trigger profile recalculation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-canonical-learner-profile-foundation/04-03-SUMMARY.md`.
</output>
