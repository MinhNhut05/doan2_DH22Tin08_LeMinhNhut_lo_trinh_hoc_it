---
phase: 04-canonical-learner-profile-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql
  - backend/src/modules/onboarding/onboarding.service.ts
  - backend/src/modules/onboarding/onboarding.service.spec.ts
  - backend/src/modules/auth/auth.service.ts
  - backend/src/modules/auth/auth.service.spec.ts
autonomous: true
requirements:
  - PROF-02
must_haves:
  truths:
    - "Round 1 onboarding answers are stored per user as round-numbered records instead of one mutable onboarding snapshot."
    - "Existing users with old onboarding data keep equivalent round-1 answers after migration instead of losing their answers."
    - "Login/new-user detection no longer depends on the removed OnboardingData model."
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "OnboardingRound and LearnerProfile schema contracts without OnboardingData"
      contains: "model OnboardingRound"
    - path: "backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql"
      provides: "Data backfill from onboarding_data into onboarding_rounds and learner_profiles"
      contains: "INSERT INTO \"onboarding_rounds\""
    - path: "backend/src/modules/onboarding/onboarding.service.ts"
      provides: "Round-1 persistence and round-based recommendation lookup"
      contains: "onboardingRound"
    - path: "backend/src/modules/auth/auth.service.ts"
      provides: "Onboarding-round-based isNewUser detection"
      contains: "roundNumber: 1"
  key_links:
    - from: "backend/src/modules/onboarding/onboarding.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "prisma.onboardingRound create/findUnique calls"
      pattern: "onboardingRound\\.(create|findUnique)"
    - from: "backend/src/modules/auth/auth.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "user include onboardingRounds filtered to completed round 1"
      pattern: "onboardingRounds"
---

<objective>
Replace the legacy single-row onboarding snapshot with round-based storage and migration-safe schema contracts that Phase 4 can build on.

Purpose: Preserve learner answers for later reuse and unblock a canonical learner profile without breaking current login and onboarding flows.
Output: Prisma schema + migration SQL, onboarding round-1 persistence, and auth/new-user detection that no longer references `OnboardingData`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-canonical-learner-profile-foundation/04-CONTEXT.md
@.planning/phases/04-canonical-learner-profile-foundation/04-RESEARCH.md
@backend/prisma/schema.prisma
@backend/src/modules/onboarding/onboarding.service.ts
@backend/src/modules/auth/auth.service.ts
@backend/src/modules/onboarding/constants/onboarding-questions.ts
@backend/src/modules/onboarding/dto/submit-onboarding.dto.ts

<interfaces>
From `backend/src/modules/onboarding/dto/submit-onboarding.dto.ts`:
```typescript
export class SubmitOnboardingDto {
  careerGoal!: CareerGoal;
  priorKnowledge!: string[];
  learningBackground!: LearningBackground;
  hoursPerWeek!: number;
}
```

From `backend/src/modules/onboarding/constants/onboarding-questions.ts`:
```typescript
export const ONBOARDING_QUESTIONS: OnboardingQuestion[] = [
  { id: 'careerGoal', type: 'single' },
  { id: 'priorKnowledge', type: 'multiple' },
  { id: 'learningBackground', type: 'single' },
  { id: 'hoursPerWeek', type: 'single' },
];
```

From `backend/src/modules/auth/auth.service.ts` current login contract:
```typescript
export interface LoginResult {
  accessToken: string;
  refreshToken: string;
  user: {
    id: string;
    email: string;
    role: UserRole;
    displayName: string | null;
    isNewUser: boolean;
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace `OnboardingData` with round/profile Prisma contracts and a deterministic backfill migration</name>
  <files>
    backend/prisma/schema.prisma
    backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql
  </files>
  <read_first>
    backend/prisma/schema.prisma
    backend/src/modules/onboarding/onboarding.service.ts
    backend/src/modules/auth/auth.service.ts
    .planning/phases/04-canonical-learner-profile-foundation/04-CONTEXT.md
    .planning/phases/04-canonical-learner-profile-foundation/04-RESEARCH.md
  </read_first>
  <action>Update `backend/prisma/schema.prisma` to implement the locked Phase 4 data-model decisions exactly. Per D-01 and D-05, add `User.onboardingRounds OnboardingRound[]` and `User.learnerProfile LearnerProfile?`; remove `User.onboardingData`. Add Prisma enums `LearnerSkillLevel { BEGINNER INTERMEDIATE ADVANCED }` and `LearnerLearningPace { SLOW NORMAL FAST }`. Add model `OnboardingRound` with exact fields `id String @id @default(uuid())`, `userId String @map("user_id")`, `roundNumber Int @map("round_number")`, `answers Json`, `completedAt DateTime @map("completed_at")`, `createdAt DateTime @default(now()) @map("created_at")`, relation to `User`, `@@unique([userId, roundNumber])`, `@@index([userId])`, `@@map("onboarding_rounds")`. Add model `LearnerProfile` per D-01/D-02/D-03 with exact fields `id String @id @default(uuid())`, `userId String @unique @map("user_id")`, `careerGoal CareerGoal @map("career_goal")`, `skillLevel LearnerSkillLevel @map("skill_level")`, `learningPace LearnerLearningPace @map("learning_pace")`, `strengths Json @default("[]")`, `weaknesses Json @default("[]")`, `preferredTopics Json @default("[]") @map("preferred_topics")`, `lastRecalculatedAt DateTime @map("last_recalculated_at")`, `createdAt DateTime @default(now()) @map("created_at")`, `updatedAt DateTime @updatedAt @map("updated_at")`, relation to `User`, `@@index([skillLevel])`, `@@map("learner_profiles")`. Do not add version-history tables or extra profile snapshots because D-03 explicitly says one row per user overwritten on recalculation. Then create `backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql` that: (1) creates the new enums/tables; (2) inserts every legacy `onboarding_data` row into `onboarding_rounds` as `round_number = 1` with `answers = jsonb_build_object('careerGoal', career_goal, 'priorKnowledge', prior_knowledge, 'learningBackground', learning_background, 'hoursPerWeek', hours_per_week)` per D-06/D-07; (3) backfills `learner_profiles` for the same users with `career_goal = onboarding_data.career_goal`, `skill_level = CASE WHEN jsonb_array_length(prior_knowledge) >= 4 AND hours_per_week >= 10 THEN 'INTERMEDIATE' ELSE 'BEGINNER' END`, `learning_pace = CASE WHEN hours_per_week <= 5 THEN 'SLOW' WHEN hours_per_week >= 15 THEN 'FAST' ELSE 'NORMAL' END`, `strengths = '[]'::jsonb`, `weaknesses = '[]'::jsonb`, `preferred_topics = COALESCE(prior_knowledge, '[]'::jsonb)`, `last_recalculated_at = completed_at`; and (4) drops `onboarding_data`. Use snake_case table/column names and explicit SQL inserts so old production data survives the migration. Do not rely on `prisma migrate reset --force` plus post-reset seeding of `onboarding_data`, because this migration removes that legacy table. Instead, prove the backfill contract with deterministic automated assertions against the migration SQL itself after Prisma schema validation/generation.</action>
  <acceptance_criteria>
    - `backend/prisma/schema.prisma` contains `model OnboardingRound {`
    - `backend/prisma/schema.prisma` contains `model LearnerProfile {`
    - `backend/prisma/schema.prisma` contains `@@unique([userId, roundNumber])`
    - `backend/prisma/schema.prisma` does not contain `model OnboardingData {`
    - `backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql` contains `INSERT INTO "onboarding_rounds"`
    - `backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql` contains `jsonb_build_object('careerGoal'`
    - `backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql` contains `INSERT INTO "learner_profiles"`
    - `backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql` contains `CASE WHEN jsonb_array_length(prior_knowledge) >= 4 AND hours_per_week >= 10 THEN 'INTERMEDIATE' ELSE 'BEGINNER' END`
    - `backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql` contains `CASE WHEN hours_per_week <= 5 THEN 'SLOW' WHEN hours_per_week >= 15 THEN 'FAST' ELSE 'NORMAL' END`
    - `backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql` contains `DROP TABLE "onboarding_data"`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend exec prisma validate && pnpm --filter backend exec prisma generate && python - <<'PY'
from pathlib import Path
sql = Path('backend/prisma/migrations/20260324_phase4_canonical_learner_profile/migration.sql').read_text()
checks = [
    'INSERT INTO "onboarding_rounds"',
    "jsonb_build_object('careerGoal'",
    'INSERT INTO "learner_profiles"',
    "CASE WHEN jsonb_array_length(prior_knowledge) >= 4 AND hours_per_week >= 10 THEN 'INTERMEDIATE' ELSE 'BEGINNER' END",
    "CASE WHEN hours_per_week <= 5 THEN 'SLOW' WHEN hours_per_week >= 15 THEN 'FAST' ELSE 'NORMAL' END",
    'DROP TABLE "onboarding_data"',
]
missing = [check for check in checks if check not in sql]
if missing:
    raise SystemExit(f'Missing migration contract(s): {missing}')
PY</automated>
  </verify>
  <done>Phase 4 has concrete Prisma contracts and a reproducible migration/backfill definition for PROF-02 with no remaining schema dependency on `OnboardingData`, and the migration SQL is automatically verified to encode the required round-1 and learner-profile backfill before the legacy table is dropped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor onboarding submission and recommendation reads to round-1 storage</name>
  <files>
    backend/src/modules/onboarding/onboarding.service.ts
    backend/src/modules/onboarding/onboarding.service.spec.ts
  </files>
  <read_first>
    backend/src/modules/onboarding/onboarding.service.ts
    backend/src/modules/onboarding/onboarding.service.spec.ts
    backend/src/modules/onboarding/constants/onboarding-questions.ts
    backend/src/modules/onboarding/dto/submit-onboarding.dto.ts
    backend/prisma/schema.prisma
  </read_first>
  <behavior>
    - Test 1: `submitAnswers(userId, dto)` creates `OnboardingRound` with `roundNumber: 1`, `answers.careerGoal`, `answers.priorKnowledge`, `answers.learningBackground`, `answers.hoursPerWeek`, and `completedAt` instead of creating `OnboardingData`.
    - Test 2: duplicate round-1 submission for the same user still throws `ConflictException('Onboarding already completed')` by checking `onboardingRound.findUnique({ where: { userId_roundNumber: { userId, roundNumber: 1 } } })`.
    - Test 3: `getRecommendation(userId)` reads round-1 answers from `OnboardingRound.answers`, reconstructs the same `OnboardingDataInput`, and still falls back when AI parsing fails.
    - Test 4: no onboarding round 1 still throws `NotFoundException('Please complete onboarding first')`.
  </behavior>
  <action>Rewrite `backend/src/modules/onboarding/onboarding.service.ts` so current onboarding flow persists round 1 answers per D-05/D-07 without expanding scope into resume flow or later rounds (D-08). Replace every `prisma.onboardingData.findUnique/create` call with `prisma.onboardingRound.findUnique/create` using compound key `userId_roundNumber: { userId, roundNumber: 1 }`. Store answers exactly as `{ careerGoal: dto.careerGoal, priorKnowledge: dto.priorKnowledge, learningBackground: dto.learningBackground, hoursPerWeek: dto.hoursPerWeek }`; do not store Vietnamese labels, question text, or ad-hoc fields. Keep the existing controller/API shape unchanged so current frontend calls still work. In `getRecommendation`, load round 1, cast `answers` into the exact `OnboardingDataInput` shape, and continue using `buildOnboardingPrompt`, `parseRecommendation`, and `getFallbackRecommendation` so current recommendation behavior stays stable while raw data moves to round storage. Update `backend/src/modules/onboarding/onboarding.service.spec.ts` to assert the new Prisma method names/arguments and to verify round-1 JSON payload keys exactly match `careerGoal`, `priorKnowledge`, `learningBackground`, and `hoursPerWeek`.</action>
  <acceptance_criteria>
    - `backend/src/modules/onboarding/onboarding.service.ts` contains `userId_roundNumber`
    - `backend/src/modules/onboarding/onboarding.service.ts` contains `roundNumber: 1`
    - `backend/src/modules/onboarding/onboarding.service.ts` contains `answers: {`
    - `backend/src/modules/onboarding/onboarding.service.ts` does not contain `prisma.onboardingData.`
    - `backend/src/modules/onboarding/onboarding.service.spec.ts` contains `onboardingRound.findUnique`
    - `backend/src/modules/onboarding/onboarding.service.spec.ts` contains `onboardingRound.create`
    - `backend/src/modules/onboarding/onboarding.service.spec.ts` contains `roundNumber: 1`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend test -- onboarding.service.spec.ts --runInBand</automated>
  </verify>
  <done>Current onboarding submission/recommendation logic works against `OnboardingRound` round-1 records, preserving existing endpoint behavior while satisfying PROF-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Switch auth new-user detection from `OnboardingData` to completed round 1</name>
  <files>
    backend/src/modules/auth/auth.service.ts
    backend/src/modules/auth/auth.service.spec.ts
  </files>
  <read_first>
    backend/src/modules/auth/auth.service.ts
    backend/src/modules/auth/auth.service.spec.ts
    backend/prisma/schema.prisma
    backend/src/modules/onboarding/onboarding.service.ts
  </read_first>
  <behavior>
    - Test 1: login returns `isNewUser: true` when the user has no completed `OnboardingRound` for `roundNumber = 1`.
    - Test 2: login returns `isNewUser: false` when the user includes at least one completed `OnboardingRound` for `roundNumber = 1`.
    - Test 3: login no longer references `onboardingData` in its Prisma include or response logic.
  </behavior>
  <action>Refactor `backend/src/modules/auth/auth.service.ts` to remove the old `onboardingData` include noted in research Pitfall 1. In the `login()` user lookup, include `onboardingRounds` with `where: { roundNumber: 1, completedAt: { not: null } }`, `select: { id: true }`, and `take: 1`; then compute `isNewUser` as `user.onboardingRounds.length === 0`. Keep the existing anti-enumeration timing behavior, password checks, and response payload unchanged apart from the new source of truth. Update `backend/src/modules/auth/auth.service.spec.ts` so the mock login user uses `onboardingRounds: []` for the new-user case and `onboardingRounds: [{ id: 'round-1' }]` for the returning-user case, and assert that the service no longer expects `onboardingData` anywhere.</action>
  <acceptance_criteria>
    - `backend/src/modules/auth/auth.service.ts` contains `onboardingRounds`
    - `backend/src/modules/auth/auth.service.ts` contains `roundNumber: 1`
    - `backend/src/modules/auth/auth.service.ts` contains `isNewUser: user.onboardingRounds.length === 0`
    - `backend/src/modules/auth/auth.service.ts` does not contain `onboardingData`
    - `backend/src/modules/auth/auth.service.spec.ts` contains `onboardingRounds: []`
    - `backend/src/modules/auth/auth.service.spec.ts` contains `onboardingRounds: [{ id: 'round-1' }]`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend test -- auth.service.spec.ts --runInBand</automated>
  </verify>
  <done>Auth remains backward-compatible after the schema change, and users are classified by completed round 1 instead of the removed `OnboardingData` model.</done>
</task>

</tasks>

<verification>
Run the Prisma contract checks first, then targeted auth/onboarding tests:
- `pnpm --filter backend exec prisma validate && pnpm --filter backend exec prisma generate`
- `pnpm --filter backend test -- onboarding.service.spec.ts auth.service.spec.ts --runInBand`
</verification>

<success_criteria>
- `OnboardingData` is removed from Prisma schema and application code touched by this plan.
- A migration file exists that backfills both `onboarding_rounds` and `learner_profiles` before dropping the old table.
- Onboarding round 1 answers persist as JSON keyed by stable question ids/field names.
- Login/new-user detection passes tests using `OnboardingRound` instead of `OnboardingData`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-canonical-learner-profile-foundation/04-01-SUMMARY.md`.
</output>
