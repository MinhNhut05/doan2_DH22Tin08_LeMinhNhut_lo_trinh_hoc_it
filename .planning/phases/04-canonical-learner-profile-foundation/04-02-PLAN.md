---
phase: 04-canonical-learner-profile-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/src/modules/learner-profile/learner-profile.module.ts
  - backend/src/modules/learner-profile/learner-profile.service.ts
  - backend/src/modules/learner-profile/learner-profile.controller.ts
  - backend/src/modules/learner-profile/index.ts
  - backend/src/modules/learner-profile/learner-profile.service.spec.ts
  - backend/src/modules/learner-profile/learner-profile.controller.spec.ts
  - backend/src/app.module.ts
autonomous: true
requirements:
  - PROF-01
must_haves:
  truths:
    - "A signed-in learner can fetch one backend-owned learner profile through `GET /api/v1/learner-profile/me`."
    - "The returned profile exposes canonical computed fields from `learner_profiles` plus `roundsCompleted` derived from onboarding rounds, without leaking raw round-answer JSON."
    - "Learners without an initialized profile get an explicit not-found error instead of fabricated default profile data."
  artifacts:
    - path: "backend/src/modules/learner-profile/learner-profile.service.ts"
      provides: "Canonical learner-profile domain logic and read API source"
      contains: "getMyProfile"
    - path: "backend/src/modules/learner-profile/learner-profile.controller.ts"
      provides: "Protected GET /learner-profile/me endpoint"
      contains: "@Get('me')"
    - path: "backend/src/modules/learner-profile/learner-profile.module.ts"
      provides: "Exported learner-profile service for cross-module reuse"
      contains: "exports: [LearnerProfileService]"
  key_links:
    - from: "backend/src/modules/learner-profile/learner-profile.controller.ts"
      to: "backend/src/modules/learner-profile/learner-profile.service.ts"
      via: "controller delegates getMyProfile(userId)"
      pattern: "learnerProfileService\\.getMyProfile"
    - from: "backend/src/modules/learner-profile/learner-profile.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "prisma.learnerProfile.findUnique with onboarding round lookup"
      pattern: "learnerProfile\\.findUnique"
---

<objective>
Create the canonical learner-profile module and protected self-read API that later onboarding, recommendations, and AI features can reuse.

Purpose: Establish one backend-owned profile service as the single source of truth for learner context per D-12 and D-17.
Output: New NestJS `learner-profile` module, service, controller, tests, and app wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-canonical-learner-profile-foundation/04-CONTEXT.md
@.planning/phases/04-canonical-learner-profile-foundation/04-RESEARCH.md
@.planning/phases/04-canonical-learner-profile-foundation/04-01-PLAN.md
@backend/src/app.module.ts
@backend/src/common/decorators/current-user.decorator.ts
@backend/src/modules/dashboard/dashboard.controller.ts
@backend/src/modules/learning-paths/learning-paths.controller.ts
@backend/src/modules/auth/index.ts

<interfaces>
Controller pattern from `backend/src/modules/dashboard/dashboard.controller.ts`:
```typescript
@Controller('dashboard')
@UseGuards(JwtAuthGuard)
export class DashboardController {
  @Get('overview')
  async getOverview(@CurrentUser('id') userId: string) {
    return this.dashboardService.getOverview(userId);
  }
}
```

User decorator contract from `backend/src/common/decorators/current-user.decorator.ts`:
```typescript
export interface JwtPayload {
  id: string;
  email: string;
  role: string;
}

export const CurrentUser = createParamDecorator(
  (data: keyof JwtPayload | undefined, ctx: ExecutionContext) => {
    const request = ctx.switchToHttp().getRequest();
    const user = request.user as JwtPayload;
    return data ? user?.[data] : user;
  },
);
```

Module export pattern from `backend/src/modules/auth/index.ts`:
```typescript
export { AuthModule } from './auth.module.js';
export { AuthService } from './auth.service.js';
export { JwtAuthGuard, RolesGuard } from './guards/index.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the learner-profile module/service/controller contracts and tests</name>
  <files>
    backend/src/modules/learner-profile/learner-profile.module.ts
    backend/src/modules/learner-profile/learner-profile.service.ts
    backend/src/modules/learner-profile/learner-profile.controller.ts
    backend/src/modules/learner-profile/index.ts
    backend/src/modules/learner-profile/learner-profile.service.spec.ts
    backend/src/modules/learner-profile/learner-profile.controller.spec.ts
  </files>
  <read_first>
    backend/src/common/decorators/current-user.decorator.ts
    backend/src/modules/dashboard/dashboard.controller.ts
    backend/src/modules/learning-paths/learning-paths.controller.ts
    backend/src/modules/auth/index.ts
    backend/prisma/schema.prisma
    .planning/phases/04-canonical-learner-profile-foundation/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `getMyProfile(userId)` loads `LearnerProfile` plus onboarding round 1 and returns one object containing `careerGoal`, `skillLevel`, `learningPace`, `strengths`, `weaknesses`, `preferredTopics`, `lastRecalculatedAt`, and `roundsCompleted`.
    - Test 2: `getMyProfile(userId)` throws `NotFoundException('Learner profile not initialized')` when no `LearnerProfile` row exists, rather than inventing defaults.
    - Test 3: controller `GET me` delegates to `learnerProfileService.getMyProfile(userId)` and is guarded with `JwtAuthGuard` + `@CurrentUser('id')`.
    - Test 4: service is exported from `LearnerProfileModule` so future modules can inject it without duplicate business logic.
  </behavior>
  <action>Create a new `backend/src/modules/learner-profile/` module per D-12 and D-17. In `learner-profile.module.ts`, import `AuthModule`, register `LearnerProfileController` and `LearnerProfileService`, and export `LearnerProfileService`. In `learner-profile.controller.ts`, use `@Controller('learner-profile')`, `@UseGuards(JwtAuthGuard)`, and add exactly one route `@Get('me') async getMyProfile(@CurrentUser('id') userId: string)`. In `learner-profile.service.ts`, add public methods `async getMyProfile(userId: string)` and `async createFromRoundOne(userId: string)`; `createFromRoundOne` may be a thin helper for Phase 5 but must exist per D-04 even if only used internally later. Implement `getMyProfile(userId)` to read `learnerProfile.findUnique({ where: { userId } })` and `onboardingRound.findMany({ where: { userId }, orderBy: { roundNumber: 'asc' }, select: { roundNumber: true, completedAt: true } })`, then return this explicit shape: `{ userId, careerGoal, skillLevel, learningPace, strengths, weaknesses, preferredTopics, lastRecalculatedAt, roundsCompleted }` where `roundsCompleted` is an array of round numbers with non-null `completedAt`. If the learner profile row is missing, throw `NotFoundException('Learner profile not initialized')` to match the research recommendation for pre-onboarding behavior. Keep raw `answers` JSON out of this API because D-01 says raw answers stay in round storage while the profile exposes computed/aggregated fields. Write unit tests for both service and controller using the repo’s existing Jest + `Test.createTestingModule()` patterns.</action>
  <acceptance_criteria>
    - `backend/src/modules/learner-profile/learner-profile.module.ts` contains `exports: [LearnerProfileService]`
    - `backend/src/modules/learner-profile/learner-profile.controller.ts` contains `@Controller('learner-profile')`
    - `backend/src/modules/learner-profile/learner-profile.controller.ts` contains `@Get('me')`
    - `backend/src/modules/learner-profile/learner-profile.controller.ts` contains `@UseGuards(JwtAuthGuard)`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `async getMyProfile(userId: string)`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `async createFromRoundOne(userId: string)`
    - `backend/src/modules/learner-profile/learner-profile.service.ts` contains `Learner profile not initialized`
    - `backend/src/modules/learner-profile/learner-profile.service.spec.ts` exists and contains `describe('LearnerProfileService'`
    - `backend/src/modules/learner-profile/learner-profile.controller.spec.ts` exists and contains `describe('LearnerProfileController'`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend test -- learner-profile.service.spec.ts learner-profile.controller.spec.ts --runInBand</automated>
  </verify>
  <done>The codebase has a reusable learner-profile domain module and tests proving the canonical read contract for PROF-01.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the learner-profile module into the Nest app root</name>
  <files>
    backend/src/app.module.ts
  </files>
  <read_first>
    backend/src/app.module.ts
    backend/src/modules/learner-profile/index.ts
    backend/src/modules/auth/index.ts
  </read_first>
  <action>Update `backend/src/app.module.ts` to import `LearnerProfileModule` from `./modules/learner-profile/index.js` and register it in the main `imports` array after `OnboardingModule` and before `LearningPathsModule`. Keep the current module ordering/comments style. Do not remove or reorder unrelated existing modules beyond this small insertion. This wiring makes `GET /api/v1/learner-profile/me` available through the same API root and enables later modules to import the learner-profile provider from one explicit feature owner.</action>
  <acceptance_criteria>
    - `backend/src/app.module.ts` contains `import { LearnerProfileModule } from './modules/learner-profile/index.js';`
    - `backend/src/app.module.ts` contains `LearnerProfileModule,`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter backend exec tsc --noEmit</automated>
  </verify>
  <done>The learner-profile feature is part of the running Nest application and ready for later recalc-hook integration.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend test -- learner-profile.service.spec.ts learner-profile.controller.spec.ts --runInBand`
- `pnpm --filter backend exec tsc --noEmit`
</verification>

<success_criteria>
- A protected `GET /api/v1/learner-profile/me` endpoint exists under the Nest app.
- The endpoint is backed by one exported `LearnerProfileService`, not ad-hoc controller logic.
- The service returns explicit canonical profile fields and an explicit not-found error for uninitialized users.
</success_criteria>

<output>
After completion, create `.planning/phases/04-canonical-learner-profile-foundation/04-02-SUMMARY.md`.
</output>
